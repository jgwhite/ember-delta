
import { patchFor } from 'dummy/helpers/patch-for';
import { module, test } from 'qunit';

module('Unit | Helper | patch for');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = patchFor([42]);
  assert.ok(result);
});

